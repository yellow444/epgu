# 05 - Журнал работ

Хронология содержательных работ по проекту: что делали, какие решения приняли и
чем закончилось. Нужен для того, чтобы контекст не пришлось восстанавливать
заново. Технические подробности правок смотрите в `git log` и в
[`docs/CHANGELOG.md`](../CHANGELOG.md).

Новые записи добавляются сверху.

## 2026-08-12 - снимок каталога API и версия 0.2.0 библиотеки

Состояние на эту дату зафиксировано по рабочему дереву (изменения ещё не
закоммичены).

- `docs/api_for_gu/` - проверяемый локальный снимок публичного каталога
  [partners.gosuslugi.ru/catalog/api_for_gu](https://partners.gosuslugi.ru/catalog/api_for_gu):
  игнорируемые Git оригиналы документов в `source/`, публикуемый `catalog.json`
  с URL, датой актуализации, размером и SHA-256 каждого файла, игнорируемый
  `extracted/` с XML/XSD и inventory со связью «блок - таблица - ячейка - хэш».
- `scripts/sync_api_for_gu_docs.py`, `scripts/extract_api_for_gu_assets.py`,
  `scripts/build_service_profiles.py` - воспроизводимое обновление снимка.
- `python-epgu` версии 0.2.0: контракты синхронизированы со спецификацией
  API ЕПГУ v1.14, переработан `push_chunked`, добавлены `OrdersPage`,
  `DictionaryResult`, безопасный `AasClient.exchange_callback`, `validate_xml`,
  матрица CI для Python 3.10-3.14. Подробности в
  [`python-epgu/CHANGELOG.md`](../../python-epgu/CHANGELOG.md).
- В `api-gosuslugi-backend/certs/` появился набор сертификатов удостоверяющих
  центров (`ca-root.crt`, `rootca.cer`, `test_ca_rtk2.cer` и прочие).

## 2026-08-06 - стенд поднят, вкладка «Настройка» стала рабочей

Чтобы compose вообще стартовал, пришлось починить две вещи:

- Контейнер `api` уходил в бесконечный рестарт с ошибкой
  `python: can't open file '/app/app.py\r'`. Скрипты монтируются с хоста как
  есть, а checkout на Windows кладёт их в CRLF. Появился
  [`.gitattributes`](../../.gitattributes) с `*.sh text eol=lf`, оба скрипта
  переведены в LF.
- `ModuleNotFoundError: No module named 'config'`: Dockerfile исходники не
  копирует, всё живёт на bind-mount, а в `docker-compose.yml` был смонтирован
  только `app.py`. Добавлены монтирования `config.py` и `routers.py`.

Живая диагностика на поднятом стенде: `/hc` и `/status` отвечают
(PyCades 2.0.15000), `/services` отдаёт три услуги, `get_certificates` возвращает
ошибку «сертификатов не найдено» - папка `xxx.000` пустая, ключевого контейнера
на диске нет.

Вкладка «Настройка» расширена до девяти автоматических проверок: контур по паре
хостов, ограничение CORS через `runtime.allowed_origins`, наличие и срок маркера
доступа ЕСИА, реальный запрос `GET /.env` через nginx с тремя разными вердиктами
для «отдал секреты», «отдал заглушку» и «не отдал». Добавлена кнопка разбора
причин отказа 401. Каждый шаг инструкции получил действие: ссылку с кнопками
«Открыть» и «Ссылка», сниппет с копированием и скачиванием, письмо в модальном
окне с копированием отдельных полей, скачиванием `.eml` и ссылкой `mailto`.
Шесть шаблонов писем: тестовый сертификат, полномочие API-Key, настройка
параметров ИС, совместное тестирование, подключение к прому, инцидент.

## 2026-08-05 - живая инструкция вместо статичного текста

Задача: инструкция по подключению к ЕПГУ должна быть не текстом, а рабочим
мастером внутри приложения, с кнопками на каждой строке. Решили делать это
пунктом меню в самом приложении, а не отдельной страницей.

- Новый компонент
  [`SetupGuide.js`](../../api-gosuslugi-client/src/components/SetupGuide/SetupGuide.js):
  состояние стенда (`/hc`, `/status`, `/get_certificates`,
  `/get_current_certificate`, `/services`), генератор `.env` с переключателем
  «Тест / Прод», выбор текущего сертификата, проверка API-Key через реальный
  `POST /accessTkn_esia` с разбором токена и причин отказа, каталог услуг.
- Исправлен рабочий баг: клиент слал `cert_id` в теле запроса, а бэкенд
  объявляет его query-параметром, поэтому выбор сертификата всегда падал в 422,
  а `catch` показывал невнятное «Ошибка установки сертификата».
- В `setupTests.js` добавлена заглушка `MessageChannel`: jsdom его не
  реализует, а `@rc-component/select` через него планирует задачи, и падал любой
  тест, рендерящий `Select`.

Старая версия вкладки в HEAD не была закоммичена и жила только в рабочей копии,
её содержимое перенесено в новый компонент.

## 2026-07-31 - авторы истории и находка про `.env`

- Глобальный `git config` был выставлен на подпись со второго, рабочего
  аккаунта, из-за чего новые коммиты в любом репозитории уходили под него.
  Исправлен на `Maksim <yellow444@gmail.com>`.
- Переписаны авторы во всех локальных ветках. Дерево файлов совпало бит в бит,
  изменились только метаданные авторства. `main` при этом не изменился, поэтому
  force-push в `main` не потребовался.
- В `main` добавлен [`.mailmap`](../../.mailmap) с каноничными подписями
  (коммит `f0c76b1`, запушен).
- Виджет Contributors в сайдбаре GitHub продолжал показывать третьего
  участника, хотя REST API и график Insights отдавали двоих. Это кэш на стороне
  GitHub, чинится только обращением в поддержку. См.
  [`06-repo-and-identity.md`](06-repo-and-identity.md).
- Попутно замечено, что в публичном репозитории отслеживаются `.env` и
  `api-gosuslugi-backend/.env`. Задача была заведена отдельно и осталась
  недоделанной. Что там на самом деле, разобрано в
  [`09-open-items.md`](09-open-items.md).

## 2026-06-28 - библиотека `epgu-api`, чистка репозитория и лицензия

- Написана переиспользуемая Python-библиотека [`python-epgu/`](../../python-epgu)
  (пакет `epgu-api`, импорт `epgu`) по мотивам
  [ofstudio/go-api-epgu](https://github.com/ofstudio/go-api-epgu). Логика
  портирована из `api-gosuslugi-backend/app.py`. Относительно Go-оригинала
  добавлен гражданский поток OAuth2 ЕСИА (`AasClient`), а подпись вынесена за
  интерфейс `Signer`, поэтому пакет работает и там, где нет КриптоПро.
- Библиотека сначала была ошибочно положена в постороннюю папку
  `F:\source\repos\epgu` и перенесена в правильную. Подробности в
  [`06-repo-and-identity.md`](06-repo-and-identity.md).
- Из истории `main` убрана посторонняя подпись автора, второй участник
  (introquest) сохранён.
- Проведена сплошная чистка текстов репозитория: длинные тире заменены на
  дефис, стрелки на `->`, убраны эмодзи, значки, галочки и псевдографика.
  Затронуто 30 файлов. Перевод строк CRLF в клиентских файлах сохранён.
- Обзорные заметки `docs/context/01-04` перенесены из посторонней папки.
- Лицензия переведена с MIT на двойную: AGPL-3.0-or-later плюс коммерческая.
  См. [`07-licensing-and-publication.md`](07-licensing-and-publication.md).

## 2026-05-12 - сверка с порталом партнёров

Актуализация документации и кода под каталог API Госуслуг: `docs/SERVICES.md`,
диагностические эндпоинты `/version`, `/environments`, `/services/{code}`,
безопасный XML-парсер, переменная `ALLOWED_ORIGINS`. Полный разбор в
[`docs/CHANGELOG.md`](../CHANGELOG.md).

## 2026-04-24 и раньше

- 2026-04-24: полный комплект markdown-документации в `docs/`, сертификаты
  разделены на публичные и личные. Тогда же переписывалась история, от того
  периода осталась ветка `backup/pre-scrub-20260424`.
- 2025-03 и 2025-04: React и FastAPI, сборка бэкенда (единственный коммит
  introquest).
- 2024-01 и 2024-02: первоначальный коммит и статья для Хабра.
