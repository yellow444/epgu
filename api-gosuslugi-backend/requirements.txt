# Runtime dependency lock for the backend container (Python 3.12).
# CryptoPro/pycades is supplied by the image and is intentionally not on PyPI.
fastapi==0.141.1
uvicorn==0.52.1
python-dotenv==1.2.2
python-multipart==0.0.32
lxml==6.1.1
# SRV и MX для автоопределения почтовых серверов: в стандартной библиотеке их нет.
dnspython==2.7.0
# Разбор PDF из писем: шрифты в них с собственной кодировкой, наивное чтение
# строк даёт мусор вместо кириллицы. Библиотека читает ToUnicode и вложения.
pypdf==6.1.3
httpx==0.28.1
pydantic==2.13.4
-e ../python-epgu[xml]
